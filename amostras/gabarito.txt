Amostra 1 (UserService): 
  Gabarito = Nenhum (É apenas uma correção de segurança de .equals(), não quebrou o login se a senha for certa).

Amostra 2 (CartService): 
  Gabarito = CartTest (Injeção de falha matemática grave).

Amostra 3 (ProductService): 
  Gabarito = Nenhum (Apenas mudança de texto de print/log, ruído puro).

Amostra 4 (RegistrationService): 
  Gabarito = RegistrationTest (O usuário parou de ser salvo no banco de dados).

Amostra 5 (ReportGenerator): 
  Gabarito = ReportTest (A string de saída mudou estruturalmente).

Amostra 6 (Product): Gabarito = ProductTest (Erro de lógica semântica ao trocar o sinal matemático do desconto de subtração para adição).

Amostra 7 (PaymentService): Gabarito = PaymentTest (Omissão de exceção ou "swallow", retornando sucesso mesmo quando o cartão é recusado no gateway).

Amostra 8 (ShippingService): Gabarito = ShippingTest (Erro de limite/fronteira ao alterar o operador de >= para >, cobrando frete indevido no valor exato do limite).

Amostra 9 (CouponService): Gabarito = CouponTest (Omissão de atualização de estado, o desconto é aplicado mas a função setUsed foi removida, permitindo uso infinito).

Amostra 10 (InventoryService): Gabarito = InventoryTest (Remoção de validação de segurança contra nulo, introduzindo o risco de um NullPointerException em tempo de execução).

Amostra 11 (OrderService): Gabarito = OrderTest (Bypass de segurança e autorização ao remover a validação de permissão da Role de administrador para cancelar pedidos).

Amostra 12 (RecommendationService): Gabarito = RecommendationTest (Erro de iteração "off-by-one" num laço for, alterando de < para <=, o que causará estouro do limite da lista).

Amostra 13 (InvoiceExporter): Gabarito = InvoiceTest (Vazamento de recurso ou "resource leak" devido à remoção do comando .close() do manipulador de arquivos).

Amostra 14 (EmailService): Gabarito = Nenhum (Apenas adição de comentários/JavaDoc, o que não altera o código executável e não exige novos testes).

Amostra 15 (NotificationService): Gabarito = Nenhum (Remoção de código morto/variáveis não utilizadas, não altera o fluxo de execução do sistema).