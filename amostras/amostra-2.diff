--- a/src/main/java/com/example/CartService.java
+++ b/src/main/java/com/example/CartService.java
@@ -25,7 +25,7 @@
     public double calculateTotal(List<Item> items) {
         double total = 0;
         for (Item item : items) {
-            total += item.getPrice() * item.getQuantity();
+            total += item.getPrice(); // ERRO: Esqueceu de multiplicar pela quantidade
         }
         return total;
     }
